import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  searchButton: {
    position: 'absolute',
    right: 12
  },

  searchButtonImg: {
    position: 'relative',
    width: 20,
    height: 20
  }
});

export default styles;
